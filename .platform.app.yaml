name: app

type: 'nodejs:12'

# Small change.
hooks:
  build: |
    if [ ! -f .env ]; then
        ln -s run/.env .env
    fi

disk: 256

mounts:
  'run':
    source: local
    source_path: run

web:
  commands:
    start: "npm start"
