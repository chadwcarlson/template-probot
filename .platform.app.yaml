name: app

type: 'nodejs:12'

hooks:
  build: |
    if [ ! -f run/.platform.installed ]; then
        touch run/.platform.installed
        ln -s run/.env .env
    fi

disk: 256

mounts:
  'run':
    source: local
    source_path: run

web:
  commands:
    start: "npm start"
